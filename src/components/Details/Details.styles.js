import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    details: {
        padding: 10,
        height: '100%',
    },
    detailsTitle: {
        fontWeight: '600',
    },
    detailsDescription: {
        marginTop: 10,
        marginBottom: 10,
    }
});

export default styles;
